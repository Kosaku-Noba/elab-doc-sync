# Changelog

## v1.0.1 (2026-09-18)

### 改善

- `esync rm` の既存の複数ファイル指定に加え、ディレクトリ指定とパスのワイルドカード（glob: `*`、`?`、`[]`）に対応。例: `esync rm elab_doc/elab*`。ディレクトリに一致した場合は配下の追跡文書を再帰的に選択。
- `esync rm --regex '式'` を追加。拡張子を含むファイル名だけに正規表現で部分一致し、複数指定やパス・IDとの併用が可能。重複する文書は一度だけ処理。
- 引用符で囲んだ glob は esync が追跡情報から選択。引用符なしではシェルが展開するため、未追跡ファイルが含まれるとエラー。無一致・不正な正規表現・ターゲットの曖昧性は変更前に検出。
- 通常は追跡解除のみで文書を保持し、`--local` 指定時だけローカル文書も削除。リモートデータは保持。`esync rm 'docs/*' --local --dry-run` でサブディレクトリ配下を含む対象を事前確認可能。

### 検証・ドキュメント

- Windows の権限表現と既定文字コードに合わせ、権限保持・バックアップの既存テストを移植可能に修正。
- 複数指定、ディレクトリの再帰選択、glob、ファイル名への正規表現、dry-run、異常時の変更防止、重複排除の回帰テストを追加。
- README と [CLI リファレンス](docs/05_CLI_REFERENCE.md#rm--追跡解除) に使用例と選択範囲を追記。

## v1.0.0 (2026-09-15)

### 新機能

- ローカル・リモート・前回同期時点を比較する状態判定をpush/pull/statusで共有。競合と基準情報不足を検出して停止。
- 変更前のローカルバックアップ、`backup list`、`restore` を追加。復元前の状態も退避。
- `mv` で本文編集を伴う移動でも記事との紐付けを保持。次のpushでリモートタイトルを更新。
- 途中失敗の記録と再開、記事作成IDの保存、応答不明時の重複作成防止を追加。
- `link` で追跡解除を取り消し、`link --new` で記事が存在しないことを確認した後の新規作成を再開。

### 動作変更・移行

- 通常のpullはローカル未編集の文書を更新。ローカルだけの編集は保持し、両側の変更は自動マージしない。
- `mode: merge` を設定エラーとして拒否。既存記事を保持する移行手順は [v1.0への移行と復旧](docs/13_MIGRATION_V1.md) を参照。
- 自動リネームは本文ハッシュの一致と一意性を確認できる場合に限定。
- `status` はオンラインで確認。処理失敗・競合は終了コード1、設定・引数不正は2。
- 画像・動画・ファイルリンク・添付の送信をプロジェクト内の通常ファイルに限定。隠しパス・シンボリックリンク・プロジェクト外参照を拒否。
- 復元は保存したディレクトリまたはファイル全体に適用し、保存後の追加ファイルも除去。最初に `restore --dry-run` で範囲を確認。

### 修正

- タグなし記事の `tags: null` によるpull時の反復処理エラーに対応。タグAPIのnullも空配列として扱い、pushで最初のタグを追加できるよう修正。
- 保存済みのタグ・カテゴリ表現を正規化し、旧同期記録による不要な競合を防止。
- タグ・カテゴリ更新結果を期待値と比較し、第三者の変更を同期済み基準へ取り込まないよう修正。
- HTTP 200で返される削除済み記事（`state=3`）もリモート削除として扱い、強制pushでも更新を停止。
- 状態ファイルの原子的な保存とプロジェクト単位の排他制御を導入。復旧記録の上書きと復元後の重複作成を防止。
- サブディレクトリ内の画像の差分検出、Markdownリンクのフラグメント保持、cloneのdry-runを修正。

### 検証・ドキュメント

- 状態判定・競合・途中失敗・復元・旧形式互換・タグなし記事の回帰テストを追加。
- eLabFTW 5.5.14で本文（md/html）、画像・添付、競合・復元を検証。一時記事は削除後の状態まで確認。
- README、CLIリファレンス、移行ガイド、[リリース検証](docs/14_RELEASE_VALIDATION.md) を整備。

## v0.5.3 (2026-08-19)

### 破壊的変更

#### merge モード完全削除
- `DocsSyncer` クラスを削除（約250行削減）
- `mode` 設定は不要に（each 固定）
- 既存設定から `mode: merge` を削除してください

### 修正

#### eLabFTW 記事 URL の修正
- items: `items.php` → `database.php?mode=view&id=XX` に修正
- experiments: `experiments.php?mode=view&id=XX`（変更なし）

### ドキュメント
- docs/ を全面更新（新機能・内部仕様を詳細記載）

## v0.5.2 (2026-08-19)

### 修正

#### `esync update` 後のバージョン不一致検出
- update 完了後に PATH 上の esync が `.venv` 内の古い版でないかを自動チェック
- `.venv` 版が優先されている場合は警告と解決方法を表示
- README トラブルシューティングに原因・確認方法・解決方法・防止策を追記

## v0.5.1 (2026-08-18)

### 新機能

#### 動画埋め込み
- 動画ファイル (.mp4, .webm) を eLabFTW にアップロードし `<video>` タグに変換
- `[text](video.mp4)` / `![alt](video.mp4)` の両記法に対応

#### ファイルリンクアップロード
- 非画像・非動画ファイル (PDF, CSV 等) のリンクを eLabFTW にアップロードし URL を書き換え
- `![alt](file.pdf)` は `[alt](url)` に正規化
- `.md` ファイルリンクはファイル間リンク変換と干渉しないようスキップ

### 改善
- 処理パイプライン: images → videos → file_links → local_links の4段に拡張
- dry-run に動画件数・リンクファイル件数を表示

## v0.5.0 (2026-08-18)

### 新機能

#### ファイル間リンク自動変換（each モード）
- push 時: `[text](./file.md)` → eLabFTW 記事 URL に自動変換
- pull 時: eLabFTW 記事 URL → `[text](./file.md)` に逆変換
- フラグメント（`#section`）は保持される
- 同一 push 内の新規ファイル間リンクも正しく解決（2パス sync）

#### ファイル名リネーム検出（each モード）
- ファイル名変更を自動検出し、mapping 更新 + eLabFTW タイトル同期
- 内容ハッシュで同一性を検証（純粋リネーム / リネーム+編集を区別）
- 複数同時リネームは安全のため警告のみ

### 改善
- URL 判定を `urlparse` ホスト完全一致に変更（誤マッチ防止）
- sync を2パス構造に変更（パス1: 全ID確定、パス2: リンク変換+送信）

## v0.4.2 (2026-08-05)

### 修正

#### pull 時のターゲット間コンタミ解消
- mapping.json をターゲットごとに分離（`.elab-sync-ids/{docs_dir}/` 配下に配置）
- 複数ターゲット（例: 週報用 / その他記録用）で pull 時にファイルが混在するバグを修正
- 旧共有 mapping.json からの自動マイグレーション付き

#### esync init のデフォルト変更
- 同期モードのデフォルトを `merge` → `each` に変更
- `merge` は非推奨表記に

#### タイトル変更時リネーム
- eLabFTW 上でタイトルを変更後に pull した際、既存ファイルが正しくリネームされるようになった（mapping 分離による副次修正）

## v0.2.1 (2026-04-09)

### 新機能

#### 添付ファイル対応
- `attachments_dir` 設定で画像以外のファイル（PDF, CSV, Excel 等）を自動アップロード・ダウンロード
- push 時: 指定ディレクトリ内の非画像ファイルをリモートにアップロード（サイズ比較で差分検知・再利用）
- pull / clone 時: リモートの非画像添付ファイルをローカルにダウンロード
- dry-run で添付ファイル件数を表示
- 同名・同サイズのファイルは再利用（内容変更時は `--force` で強制再送）

### 修正

#### LaTeX 数式保護
- `body_format: html` で `$$...$$` / `$...$` の LaTeX 数式が壊れる問題を修正
- 数式内の `<`, `>`, `&` を HTML エンティティに変換
- `\$` はリテラルドル記号として扱い、数式開始とみなさない

#### pull 時の過剰エスケープ抑制
- `markdownify` による `\_` `\*` 等の過剰エスケープを抑制
- push → pull → edit → push のラウンドトリップが安定

> 検証: 220 テスト通過（S-85〜S-104, CLI-54〜CLI-55 で数式保護・添付ファイル・エスケープ抑制をカバー）

## v0.2.0 (2026-04-06)

### 新機能

#### FR-11: 競合検出
- push 前にリモートの body ハッシュを前回同期時と比較し、リモートが変更されていたら push を中断
- `--force` で競合を無視して強制上書き可能
- pull 成功時・push 成功時に remote_hash を自動保存

#### FR-12: Clone
- `esync clone --url <url> --id <id>` でリモートからローカルプロジェクトを一発構築
- 複数 ID 対応（`--id 42 --id 43`）
- 全件取得失敗時のクリーンアップ、既存ディレクトリ保護

#### FR-13: 同期ログ
- push/pull の操作履歴を JSONL 形式で記録
- `esync log [--limit N]` で表示
- best-effort 設計（ログ書き込み失敗が同期を中断しない）

#### FR-15: タグ・メタデータ管理
- `esync tag list/add/remove` — リモートのタグを CLI から操作
- `esync metadata get/set` — メタデータを key=value 形式で管理
- `.elab-sync.yaml` の `targets[].tags` で push 時にタグを自動追記（追記のみ、既存タグは外さない）

#### FR-16: ステータス管理
- `esync entity-status show/set` — エンティティのステータスを表示・変更
- 複数エンティティ対象時は確認プロンプトを表示
- `--id` で単体指定可能

#### FR-17: マルチユーザー対応（whoami）
- `esync whoami` — 現在の API キーに紐づくユーザー名・メール・チームを表示
- 同期ログの `record()` に `user` パラメータを追加（API のみ。通常の push/pull では未使用）

#### FR-18: テンプレート機能
- `esync new --list` — eLabFTW のテンプレート一覧を表示
- `esync new --template-id <ID>` — テンプレートから Markdown ファイルを生成
- `--title`, `--output` オプション対応

#### FR-19: リモート一覧表示
- `esync list [--entity experiments] [--limit N]` — リモートのリソース/実験ノート一覧を表示

#### FR-20: 手動紐付け
- `esync link <ID> [--file name.md]` — 既存リモートエンティティとローカルを手動で接続
- 紐付け時に remote_hash を初期化（競合検出のベースライン設定）

#### FR-21: 整合性チェック
- `esync verify` — ローカルファイルとリモートエンティティの接続状態を検証

### 改善

#### pull の安全性向上
- 初回 pull で `--id` 必須に変更（全件ダウンロード廃止）
- `--id` を複数指定可能に（`--id 42 --id 43`）
- `--entity` オプションで items/experiments を明示指定可能
- merge モードで複数 `--id` 指定時は警告を表示

#### 用語統一: アイテム → リソース
- CLI のユーザー向け表示を「アイテム」から「リソース」に変更（eLabFTW Web UI の表示名に統一）
- `--entity resources` を `items` のエイリアスとして受け付け（API 引数の `items` は引き続き有効）
- `.elab-sync.yaml` の `entity: resources` も `items` に正規化

#### Codex 自動レビュー基盤
- post-commit フックで Codex による6観点レビューを自動実行
- 日本語出力を強制するカスタムプロンプト
- `SKIP_CODEX_REVIEW=1` でスキップ可能
- レビュー時に `pytest` を自動実行

### テスト

- テストケース: 0 → 138 件
- GitHub Actions で Python 3.10/3.12 の自動テスト
- 全 API 通信は mock、ファイル操作は `tmp_path` で完結

### ドキュメント

- 番号付きドキュメント体系（01〜12）を新設
- CLI リファレンス、API リファレンス、設定ファイル仕様、同期エンジン仕様、テスト仕様書、設計判断記録
- AI レビュー方法論ドキュメント
- template/README.md を初見ユーザー向けに全面改訂

### 破壊的変更

- `esync pull` の `--id` なし初回実行で全件ダウンロードしなくなりました。`--id` を指定してください。
- CLI の表示が「アイテム」から「リソース」に変更されました（`items` は引き続き `--entity` の値として使用可能）。

### 依存関係

変更なし（requests, markdown, markdownify, pyyaml, urllib3）

---

## v0.1.0 (2026-04-05)

初回リリース。

- Push 同期（差分検知、`--force`、`--dry-run`、`-t`）
- 同期モード（merge / each）
- エンティティ種別（items / experiments）
- 画像の自動アップロード・URL 書き換え
- Pull（eLabFTW → ローカル、`--id`、`--force`）
- Diff（unified diff 形式）
- Status（同期状態確認）
- Init（対話的セットアップ）
- Update（自動更新）
- エイリアス（`esync` / `elab-doc-sync`）
